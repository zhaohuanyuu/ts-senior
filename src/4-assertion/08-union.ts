import Vechile from '../3-Vechiles/Vechile'
import Car from '../3-Vechiles/Car'
import Truck from '../3-Vechiles/Trunk'

class Customer {
  rentVechile(vechile: Truck | Car) {
    // vechile.calculateRent()
    //vechile as unknown
    // (vechile as Truck).checkIsOverWeight(false)
  }
}
